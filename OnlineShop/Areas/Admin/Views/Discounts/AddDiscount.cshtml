@model IEnumerable<OnlineShop.Models.Manufacturer>

@{ ViewData["Title"] = "Thêm giảm giá";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@*<select class="text_field js-example-basic-single" id="card_number"name="state">
    <option value="AL">Alabama</option>
    <option value="WY">Wyoming</option>
    <option value="WY">1</option>
    <option value="WY">2</option>
    <option value="WY">3</option>
    <option value="WY">4</option>
    <option value="WY">5</option>

</select>*@
<div class="dashboard_contents">
    <div class="container">
        <form action="@Url.Action("AddDiscount", "Discounts", new { area = "Admin" })" method="post">
            <div class="credit_modules">
                <div class="row">
                    <div class="col-md-12">
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="payment_info modules__content">
                            <label for="name">Chọn hãng để áp dụng giảm giá</label>
                            <div class="row">
                                <div class="col-12">
                                    <div class="form-group">
                                        <div class="select-wrap select-wrap2">
                                            <select class="js-example-basic-single" name="ManufacturerId">
                                                <option disabled selected>--Vui lòng chọn--</option>
                                                @foreach (var item in Model)
                                                {
                                                    <option value="@item.ManufacturerId">@item.ManufacturerName</option>
                                                }
                                            </select>
                                            <span class="lnr lnr-chevron-down"></span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="form-group">
                                <label for="card_number">Phần trăm giảm giá</label>
                                <input name="DiscountPercent" id="card_number" type="number" class="text_field" placeholder="Vui lòng số lượng được giảm giá...">
                            </div>
                            <div class="form-group">
                                <label for="card_number">Số lượng</label>
                                <input name="AmountOf" id="card_number" type="number" class="text_field" placeholder="Vui lòng nhập phần trăm giảm giá...">
                            </div>
                            <div class="right__inputWrapper">
                                <label for="code">Ngày bắt đầu</label>
                                <input type="datetime-local" name="StartDate">
                            </div>
                            <div class="right__inputWrapper">
                                <label for="code">Ngày kết thúc</label>
                                <input type="datetime-local" name="EndDate">
                            </div>
                            <div class="form-group">
                                <label for="authbio">Mô tả</label>
                                <textarea name="note" id="authbio" class="text_field" placeholder="Một vài mô tả ngắn gọn về sản phẩm..."></textarea>
                            </div>


                            <div class="row">
                                <div class="col-md-6">

                                    <button type="submit" class="btn btn--round btn--default">Thêm</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>